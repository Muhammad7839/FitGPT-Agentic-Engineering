# ADR Evidence Matrix

This matrix proves that the ADRs cite concrete evidence rather than only preferences. Some ADRs were formalized during the capstone from measured evidence; the table keeps that limitation explicit.

| ADR | Decision | Rejected alternative | Concrete evidence cited | Commit/eval/run reference | Consequence | Open risk |
|---|---|---|---|---|---|---|
| `0001-quality-rubric-and-evaluation-design.md` | Use a 16-point rubric with route, governance, and evidence dimensions. | Single pass/fail result; cost-only comparison. | LOW control was correct but over-served: `14/16`, route appropriateness `2/4`, cost `$0.6066006`; AURA LOW reached `16/16` at `$0.3377550`; aggregate improved `44/48 -> 48/48`. | Control evidence through `75dd1f8`; AURA evidence through `6b52ecb`; `docs/capstone/control-vs-aura-impact.md`. | Quality and route fit can be compared without pretending cost is the only objective. | Calibrated to three representative scenarios only. |
| `0002-memory-context-and-prompt-architecture.md` | Keep role prompts narrow and treat memory as context support, not proof. | Broad shared context; memory-only evidence. | Calibration run `CAL-20260802-002` preserved an output-contract failure; HIGH used separate role artifacts without authorization denials. | `docs/calibration-log.md`; `docs/agents/backend-config-docs-auditor/lab/iteration-log.md`; `.eval-artifacts/capstone/aura-runs/AF-HIGH-001/`. | Repository artifacts stay authoritative and role evidence is auditable. | Long runs can still mix stale attempts unless final sanity checks are performed. |
| `0003-mcp-tool-boundaries-and-least-privilege.md` | Enforce role/tool boundaries in MCP policy and tests. | Prompt-only boundaries; broad task-tracker or shell access for implementation roles. | Permanent policy/MCP/coursetools checks exist; real denial `GO-20260811-001` blocked `implementer -> task_tracker`. | `eval/test_policy.py`; `eval/test_mcp_runtime.py`; `eval/test_coursetools_runtime.py`; `eval/test_governance_overreach.py`; `docs/capstone/governance-overreach-demo.md`. | Least privilege is demonstrated by machine evidence, not claimed. | The `task_tracker` integration is the course simulator, not a live external tracker. |
| `0004-adaptive-risk-routing.md` | Use deterministic `aura-risk-v1` and frozen route plans per tier. | Model-decided risk routing; always full route; always minimize cost. | LOW roles dropped `5 -> 2`, checkpoints `2 -> 0`, quality `14/16 -> 16/16`; HIGH retained `5` roles and `2` checkpoints. | Router frozen at `c844db1b457712d4c68c9353c49e8bd9fd2121a1`; `eval/test_risk_classifier.py`; `eval/test_adaptive_router.py`; `docs/capstone/aura-results.md`. | Route choice is reproducible and risk-sensitive. | Future HIGH categories may need additional gates. |
| `0005-governance-and-eval-gated-ci.md` | Add a capstone governance CI workflow with policy, eval, integrity, advisory, and audit jobs. | Replace existing app tests; make AI review blocking; skip audit on failure. | Local YAML parsing passed; pipeline checker rejects weak policy gates, broad write permissions, production markers, and unsafe secret exposure; governed Docker checks reported `18 passed`. | Commit `a102a79`; `scripts/check-pipeline-integrity.py`; `eval/test_pipeline_integrity.py`; run `31527786959` passed after presentation repair. | Permanent gates work without optional AI secrets; advisory review degrades safely. | GitHub runner warnings about Node action deprecation are external and non-blocking. |
| `0006-agent-to-deterministic-conversion.md` | Convert the stable `DATABASE_URL` docs/config consistency check to deterministic code. | Convert the whole auditor; call the original risk classifier the conversion. | Run 5 had factual precision weaknesses; deterministic script emits JSON at `$0` model cost and about `0.156 ms` local runtime; pass/fail tests cover behavior. | Commit `9f9e461`; `scripts/check-config-docs-consistency.py`; `eval/test_config_docs_consistency.py`; `docs/capstone/deterministic-conversion.md`. | Stable factual guardrails are cheaper, faster, and easier to audit than a model review. | It covers one narrow fact, not the whole backend config surface. |
| `0007-change-passport-evidence-design.md` | Build Change Passport as deterministic evidence aggregation. | Manually filled Passport fields; add CI fields before real runs; make Passport a router. | Builder reads real classifier/router, metrics, quality, approvals, policy tests, CI fields, artifact hashes; missing producers fail tests instead of fabricating fields. | Commit `13edafc`; `eval/test_change_passport.py`; `docs/capstone/evidence/change-passport-AF-HIGH-001.json`; run `31513596822` CI fields. | Readiness becomes portable and machine-checkable. | CI fields should only be refreshed from real GitHub producers. |
| `0008-governed-semantic-retrieval.md` | Add deterministic semantic-vector ranking, classification ceilings, and citations to the existing retrieval server. | External embeddings; exact substring only; uncited snippets. | Related-term ranking, citation, ceiling, and empty-query behavior tests. | `mcp-servers/retrieval/server.py`; `eval/test_retrieval_behavior.py`; `docs/capstone/retrieval-tool-evidence.md`. | Semantic retrieval is offline, reproducible, and policy-ready. | Synthetic corpus is not production scale. |
| `0009-offline-verification-and-execution-limits.md` | Add a network-disabled grader container and deterministic timeout/retry/budget decisions without altering frozen routes. | Paid reruns; frozen-router mutation; documentation-only claims. | Sandbox contract and reliability behavior tests; fresh offline container run `32 passed`; permanent CI wiring. | `scripts/run-offline-governance-verification.sh`; `eval/test_sandbox_contract.py`; `eval/reliability_controls.py`; `eval/test_reliability_controls.py`. | Stronger reproducibility and machine-checkable reliability. | Historical model-backed runs used bridge networking. |
